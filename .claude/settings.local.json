{
  "permissions": {
    "allow": [
      "Bash(python -m http.server 3001)",
      "Bash(/bin/sh -c \"ls -la /workspace/.gitignore 2>/dev/null; cat /workspace/.gitignore 2>/dev/null || echo 'No .gitignore found'\")"
    ]
  }
}
